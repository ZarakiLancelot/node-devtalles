{
  "devDependencies": {
    "@types/node": "^20.11.8",
    "nodemon": "^3.0.3",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "name": "03-typescript",
  "version": "0.0.1",
  "description": "\"Uso de TypeScript en Node.Js\"",
  "main": "app.ts",
  "dependencies": {
    "undici-types": "^5.26.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node ./dist/app.js"
  },
  "author": "Edwin Vásquez Velásquez",
  "license": "ISC"
}
